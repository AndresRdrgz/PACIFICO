{
  "name": "pacifico-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for PACIFICO workflow system",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:notes": "playwright test tests/notes-reminders-e2e.spec.ts",
    "install": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
